import { StudentProfileForm } from 'src/features/student/components/StudentProfileForm'

const StudentProfile = () => {
  return <StudentProfileForm />
}

StudentProfile.acl = {
  subject: 'student'
}

export default StudentProfile
